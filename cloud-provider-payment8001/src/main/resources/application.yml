server:
  port: 8001

spring:
  application:
    name: cloud-provider-payment8001


  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: com.mysql.cj.jdbc.Driver              # mysql驱动包
    url: jdbc:mysql://localhost:3306/db2019?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&userSSL=false
    username: root
    password: 19981217



mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: com.bihai.entities # 所有Entity别名类所在包

